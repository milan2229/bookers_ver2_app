<div class="container">
  <% if @book.errors.any? %>
  <h3><%= @book.errors.count %> errors prohibited this obj from being saved:</h3>
  <div id="error_explanation" class="alert alert-danger">
    <ul>
      <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
  <div class="row">
    <div class="col-lg-6 col-lg-offset-3">
      <h2>Editing Book</h2>
      <%= form_for(@book) do |f| %>
      <label>Title</label>
        <br>
        <%= f.text_field :title %>
        <br>
      <label>opinion</label>
        <br>
        <%= f.text_area :body %>
        <br>
        <br>
        <br>
        <div class="actions">
          <%= f.submit 'Update Book', class: "btn-sm btn-success" %>
          </div>
          <br>
            <td><%= link_to "Show", book_path %></td>
            <td>|</td>
            <td><%= link_to "Back", books_path %></td>
          <% end %>
      </div>
      
          <p style="padding-left:500px;">CopyRight Infratop.inc</p>
      </div>
    </div>
</div>